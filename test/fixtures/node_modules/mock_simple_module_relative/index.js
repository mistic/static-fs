'use strict';

const fileContentBuffer = require('./read_content_file').contentBuffer;
const fileContentEnconded = require('./read_content_file').contentEncoded;

async function run() {
  console.log(fileContentBuffer.toString('utf8'));
  console.log(fileContentEnconded);
  console.log('mock_simple_module_relative has run');
}

module.exports.run = run;
