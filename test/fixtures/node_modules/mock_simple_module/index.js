'use strict';

async function run() {
  console.log('mock_simple_module has run');
}

process.on('message', async (m) => {
  if (m === 'exec->run()') {
    await run();
    process.exit(0);
  }
});

module.exports.run = run;
